connector:
  name: soy-delivery-mongo-source-arg
  config:
    connector.class: com.mongodb.kafka.connect.MongoSourceConnector
    tasks.max: '10'
    connection.uri: mongodb://userApp:Xpr.QAARG.PassRoot@srv-qa-sp-xpress-arg-mongo1:27017,srv-qa-sp-xpress-arg-mongo2:27017,srv-qa-sp-xpress-arg-mongo3:27017/admin?authSource=admin
    errors.tolerance: all
    pipeline: '[{"$match": {"ns.coll": "soyDeliveryOrder"}}]'
    flush.size: '100'
    rotate.interval.ms: '1000'
    topic.namespace.map: '{"*": "ARG-soy-delivery-mongo-changes"}'
    change.stream.full.document: updateLookup
    output.format.key: json
    output.format.value: json
    output.schema.infer.value: 'true'
