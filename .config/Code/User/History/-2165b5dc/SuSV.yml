spring:
  application:
    name: vault-demo

  cloud:
    vault:
      uri: http://127.0.0.1:18200
      authentication: NONE
      kv:
        enabled: true
        backend: secrets/GEOSwitch/qa
        default-context: puntored-service
        profile-separator: "-"